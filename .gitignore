.DS_Store
.env
.env.local
.env*.local

node_modules/
.next/
coverage/
dist/
*.tsbuildinfo
next-env.d.ts

# Local env backups — these hold real credentials.
.env.bak*
.env.*.bak

# Python virtualenv used for local tooling
.venv/

# Runtime uploads land on the service's own disk; they are not source.
public/uploads/
