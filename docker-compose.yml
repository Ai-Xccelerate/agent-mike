services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ai_worker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ai-worker-postgres:/var/lib/postgresql/data

volumes:
  ai-worker-postgres:
