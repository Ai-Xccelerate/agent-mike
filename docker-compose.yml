services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mike
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - mike-postgres:/var/lib/postgresql/data

volumes:
  mike-postgres:
