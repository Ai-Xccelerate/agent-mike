# Legacy FastAPI API

This directory is the original Python API. The live API for `staging` is the Next.js app at the repository root (`app/api`, `lib/`, `db/`). Do not point Railway `api` at `apps/api` anymore.
